{
  "rules": {
    "userData": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "profile": {
          ".indexOn": ["email"]
        },
        "notifications": {
          ".indexOn": ["createdAt", "read"]
        },
        "sharedLists": {
          ".indexOn": ["ownerId"]
        },
        "shoppingLists": {
          ".indexOn": ["name"]
        }
      }
    },
    "shoppingLists": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "sharedLists": {
      ".read": "auth != null",
      "$listId": {
        "$userId": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    },
    "invitations": {
      "$invitationId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
