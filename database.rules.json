{
  "rules": {
    "userData": {
      "$uid": {
        ".read": "auth != null",
        ".write": "$uid === auth.uid",
        "profile": {
          ".write": "$uid === auth.uid",
          ".indexOn": ["email"]
        },
        "notifications": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid",
          ".indexOn": ["createdAt", "read"]
        },
        "sharedLists": {
          ".indexOn": ["ownerId"]
        },
        "shoppingLists": {
          ".indexOn": ["name"]
        }
      }
    },
    "shoppingLists": {
      ".read": "auth != null",
      ".write": "auth != null"
    },
    "sharedLists": {
      ".read": "auth != null",
      "$listId": {
        "$userId": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    },
    "invitations": {
      "$invitationId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
